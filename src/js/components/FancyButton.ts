export class FancyButton {
    
    constructor(_dom:HTMLElement) {
        // console.log(_dom);

        /* _dom.addEventListener('mouseover', (e) => {
            _dom.parentElement.style.background = "red";
        }); */
    }
}