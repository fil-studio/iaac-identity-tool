---
layout: base
pageTemplate: landing
eleventyComputed:
  title: "{{ settings.title }}"
  description: "{{ settings.description }}"

---

{% import '../components/buttons.njk' as Buttons with context %}

<div class="gl"></div>

<div class="ui">
  <div class="logo"></div>
  <div class="menu">
    {{ Buttons.standardButton("Hola que tal", true) }}
  </div>
</div>